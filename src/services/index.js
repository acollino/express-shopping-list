// While this organization is not required for this project, I wanted to
// practice using it to prepare for more complex projects.

const shoppingListService = require("./shopping_list_service");

module.exports = { shoppingListService };
