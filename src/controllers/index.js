/**
 * While this organization is not required for this project, with only 1
 * controller file, I wanted to practice using it to prepare for
 * more complex projects.
 */

const shoppingListController = require("./shopping_list_controller");

module.exports = { shoppingListController };
